# OpenAI API Configuration
# Required: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxx

# Database Configuration
# Required: PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@db:5432/hr_db

# Application Environment
# Optional: development, staging, production
PYTHON_ENV=development

# CORS Configuration
# Optional: Comma-separated list of allowed origins
# Note: Backend runs on port 9000 (changed from 8000 due to Windows port reservation)
ALLOWED_ORIGINS=http://localhost:5173

# Logging Configuration
# Optional: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Database Read-Only User Password
# Optional: Password for the read-only database user (query_app_readonly)
# Default: readonly_secure_pass_2025 (change for production!)
READONLY_DB_PASSWORD=readonly_secure_pass_2025
